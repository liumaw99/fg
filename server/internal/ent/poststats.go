// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"social-server/internal/ent/poststats"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
)

// PostStats is the model entity for the PostStats schema.
type PostStats struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// PostID holds the value of the "post_id" field.
	PostID uuid.UUID `json:"post_id,omitempty"`
	// LikeCount holds the value of the "like_count" field.
	LikeCount int `json:"like_count,omitempty"`
	// ReplyCount holds the value of the "reply_count" field.
	ReplyCount int `json:"reply_count,omitempty"`
	// RepostCount holds the value of the "repost_count" field.
	RepostCount int `json:"repost_count,omitempty"`
	// BookmarkCount holds the value of the "bookmark_count" field.
	BookmarkCount int `json:"bookmark_count,omitempty"`
	// ViewCount holds the value of the "view_count" field.
	ViewCount int `json:"view_count,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt    time.Time `json:"updated_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*PostStats) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case poststats.FieldLikeCount, poststats.FieldReplyCount, poststats.FieldRepostCount, poststats.FieldBookmarkCount, poststats.FieldViewCount:
			values[i] = new(sql.NullInt64)
		case poststats.FieldCreatedAt, poststats.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case poststats.FieldID, poststats.FieldPostID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the PostStats fields.
func (ps *PostStats) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case poststats.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				ps.ID = *value
			}
		case poststats.FieldPostID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field post_id", values[i])
			} else if value != nil {
				ps.PostID = *value
			}
		case poststats.FieldLikeCount:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field like_count", values[i])
			} else if value.Valid {
				ps.LikeCount = int(value.Int64)
			}
		case poststats.FieldReplyCount:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field reply_count", values[i])
			} else if value.Valid {
				ps.ReplyCount = int(value.Int64)
			}
		case poststats.FieldRepostCount:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field repost_count", values[i])
			} else if value.Valid {
				ps.RepostCount = int(value.Int64)
			}
		case poststats.FieldBookmarkCount:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field bookmark_count", values[i])
			} else if value.Valid {
				ps.BookmarkCount = int(value.Int64)
			}
		case poststats.FieldViewCount:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field view_count", values[i])
			} else if value.Valid {
				ps.ViewCount = int(value.Int64)
			}
		case poststats.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				ps.CreatedAt = value.Time
			}
		case poststats.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				ps.UpdatedAt = value.Time
			}
		default:
			ps.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the PostStats.
// This includes values selected through modifiers, order, etc.
func (ps *PostStats) Value(name string) (ent.Value, error) {
	return ps.selectValues.Get(name)
}

// Update returns a builder for updating this PostStats.
// Note that you need to call PostStats.Unwrap() before calling this method if this PostStats
// was returned from a transaction, and the transaction was committed or rolled back.
func (ps *PostStats) Update() *PostStatsUpdateOne {
	return NewPostStatsClient(ps.config).UpdateOne(ps)
}

// Unwrap unwraps the PostStats entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ps *PostStats) Unwrap() *PostStats {
	_tx, ok := ps.config.driver.(*txDriver)
	if !ok {
		panic("ent: PostStats is not a transactional entity")
	}
	ps.config.driver = _tx.drv
	return ps
}

// String implements the fmt.Stringer.
func (ps *PostStats) String() string {
	var builder strings.Builder
	builder.WriteString("PostStats(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ps.ID))
	builder.WriteString("post_id=")
	builder.WriteString(fmt.Sprintf("%v", ps.PostID))
	builder.WriteString(", ")
	builder.WriteString("like_count=")
	builder.WriteString(fmt.Sprintf("%v", ps.LikeCount))
	builder.WriteString(", ")
	builder.WriteString("reply_count=")
	builder.WriteString(fmt.Sprintf("%v", ps.ReplyCount))
	builder.WriteString(", ")
	builder.WriteString("repost_count=")
	builder.WriteString(fmt.Sprintf("%v", ps.RepostCount))
	builder.WriteString(", ")
	builder.WriteString("bookmark_count=")
	builder.WriteString(fmt.Sprintf("%v", ps.BookmarkCount))
	builder.WriteString(", ")
	builder.WriteString("view_count=")
	builder.WriteString(fmt.Sprintf("%v", ps.ViewCount))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(ps.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(ps.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// PostStatsSlice is a parsable slice of PostStats.
type PostStatsSlice []*PostStats
